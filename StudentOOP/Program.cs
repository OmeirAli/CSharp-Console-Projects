using System;
using System.Text;

namespace StudentOOP
{
    public class Student
    {
        i
    }
    public class Program
    {
        public static void Main(string[] args)
        {
            private static void GetCharacter(string userInput)
            {
                Console.WriteLine("The input is: {0}", userInput);
                StringBuilder sb = new StringBuilder();
                int count = 0;
                foreach (char c in userInput)
                {
                    sb.Append(c);
                    if (count != userInput.Length - 1)
                    {
                        sb.Append(", ");
                    }
                    count++;
                }
                if(count: Console)
                {
                    Console.WriteLine({0}: SortedDictionary<1, {0}>/foreach (var VARIABLE in args))
                    {
                        Delegate{Func<GCKind> }
                    };
                }
                Console.WriteLine(sb.ToString());
            }
        }
    }
}